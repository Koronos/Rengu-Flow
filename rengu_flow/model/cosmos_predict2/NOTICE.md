# Cosmos Predict2 incorporated code

- `dit.py` — derived from NVIDIA Cosmos Predict2 modeling (Apache-2.0), originally `cosmos_predict2_modeling.py` in diffusion-pipe.
- `llm_adapter.py` — derived from NVIDIA (Apache-2.0).
- `wan_vae.py` — derived from Alibaba Wan Team VAE (see file header).

Tokenizer assets under `assets/` are config/tokenizer files from diffusion-pipe layouts.
