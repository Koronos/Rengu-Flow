# renga.local.toml.example — copy to renga.local.toml (repo root).
# Model checkpoints: set paths in your training TOML under [model], not here.

[ui]
host = "127.0.0.1"
port = 8765
data_dir = ".renga-flow-ui"
# token = "change-me"

[maintenance]
enabled = false
allow_pip = false

# Defaults for `renga train` (DeepSpeed launcher + trainer CLI).
# CLI flags override these values.
[training]
num_gpus = 1
master_port = 29500
# Space-separated args passed to renga_flow.main after --config
extra_args = ""

# Training subprocess environment (literal os.environ keys; all values are strings).
# Default: empty — opt in per machine/GPU. See docs/user/cli.md
[training.env]
# Uncomment only what you need (consumer / RTX 40xx examples):
# NCCL_P2P_DISABLE = "1"
# NCCL_IB_DISABLE = "1"
# PYTORCH_CUDA_ALLOC_CONF = "expandable_segments:True"
# RENGA_TUNING_TF32_APPLY = "1"   # Ampere+ only; enables TF32 matmul/cudnn in renga_flow.main
# CUDA_VISIBLE_DEVICES = "0"
