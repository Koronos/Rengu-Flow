/** User-facing copy for the signal controls section (see docs/user/signal-files.md). */

export const SIGNAL_DOC_PATH = "docs/user/signal-files.md";

export const SIGNAL_SECTION_HINT =
  "Touch signal files in the run folder. The trainer reacts on the next training step (or every few seconds while paused for disk export). Only affects an active training process.";
