# Minimal dataset for Cosmos Predict2 / Anima image training (frame_buckets = [1]).

resolutions = [512]
enable_ar_bucket = true
min_ar = 0.5
max_ar = 2.0
num_ar_buckets = 4
frame_buckets = [1]

[[directory]]
path = 'path/to/images'
num_repeats = 1
