# Adopted stack (2026-05-26) — upgrade smokes batch1–5 OK on torqastl (baseline iter 0.712s, 6.51 GB).
# Python 3.12.9 venv. Base deps: uv pip install -r requirements.txt
# PyTorch cu130 wheels (torch 2.12 no longer publishes cu128):
#   uv pip install torch==2.12.0 torchvision==0.27.0 --index-url https://download.pytorch.org/whl/cu130
# Then: uv pip install -r requirements-pinned.txt
# diffusers 0.38 needs safetensors 0.8 rc: uv pip install --prerelease=allow safetensors>=0.8.0

torch==2.12.0
torchvision==0.27.0
deepspeed==0.18.9
transformers==5.9.0
diffusers==0.38.0
accelerate==1.13.0
peft==0.19.1
datasets==4.8.5
numpy==2.3.5
scipy==1.17.1
safetensors==0.8.0rc0
bitsandbytes==0.49.1
comfy-aimdo==0.4.0
comfy-kitchen==0.2.8
